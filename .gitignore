# Obsidian 配置包 .gitignore
# 原则：只追踪配置与文档；插件本体、运行时文件、任何笔记一律不入库

# --- 全部忽略，然后白名单放行（防笔记泄漏的写法）---
/*

# 允许入库
!.gitignore
!README.md
!ATTRIBUTION.md
!LICENSE
!/docs/
!/scripts/
!/templates/
!/.obsidian/
.obsidian/*
!.obsidian/appearance.json
!.obsidian/community-plugins.json
!.obsidian/core-plugins.json
!.obsidian/graph.json
!.obsidian/hotkeys.json
!.obsidian/snippets/
!.obsidian/plugins/

# 插件本体与运行时文件（即使被复制进来也不入库）
.obsidian/workspace.json
.obsidian/workspace-*.json
.obsidian/themes/
.obsidian/app.json
.obsidian/bookmarks.json
.obsidian/plugins/*/main.js
.obsidian/plugins/*/styles.css
.obsidian/plugins/*/manifest.json
.obsidian/plugins/*/data.json.backup

# 含 vault 路径 / 打开记录，属运行时状态而非配置，不入库：
# - flexplorer: items 是 1000+ 条笔记与文件夹路径（排序、隐藏、固定状态）
# - crisp-file-explorer: activity 是今日与常用文件记录 + 每篇的打开次数
# - crisp-reading-rail: readingMemory 是每篇笔记的阅读进度与当前标题
#   （该插件已停用，功能由自研 reading-rail-sidebar 接管）
# 本地文件照常保留，仅不备份到远端；真正的配置项已抄录进 docs/plugins.md。
.obsidian/plugins/flexplorer/data.json
.obsidian/plugins/crisp-file-explorer/data.json
.obsidian/plugins/crisp-reading-rail/data.json

# 例外：自研插件整体入库（其余插件的 data.json 设置仍按上面规则放行）
# 注意：这一组必须写在上面那批通配排除【之后】。
# gitignore 是后置规则优先，白名单若写在前面，会被后面的
# .obsidian/plugins/*/main.js 等规则重新盖掉 —— 之前 toolbar-pin-toggle
# 就是这样只入库了 data.json，main.js / styles.css / manifest.json 全漏了。
!.obsidian/plugins/toolbar-pin-toggle/
!.obsidian/plugins/toolbar-pin-toggle/**
!.obsidian/plugins/reading-rail-sidebar/
!.obsidian/plugins/reading-rail-sidebar/**

# 大文件 / 敏感信息
*.zip
*.rar
*.key
*.pem
.env*
*.log
